apiVersion: v1
kind: Service
metadata:
  name: linux-nginx
  labels:
    app: linux-nginx
spec:
  selector:
    app: linux-nginx
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: ClusterIP
